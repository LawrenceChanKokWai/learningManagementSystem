let express,cors,readdirSync,mongoose;_40e‍.w("express",[["default",["express"],function(v){express=v}]]);_40e‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_40e‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_40e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require("dotenv").config();

// create express app
const app = express();

// db
mongoose
	.connect(process.env.DATABASE, async (err) => {
		if (err) throw err;
		_40e‍.g.console.log("DB CONNECTION ERR => ", err);
	})
	.then(() => console.log("**DB CONNECTED**"));

// apply middlewares
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));

// route
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

// port
const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));

mongoose.connect(
	MONGODB_URL,
	{
		useCreatendex: true,
		useFindAndModify: false,
		useNewUrlParser: true,
		useUnifiedTopology: true,
	},
	(err) => {
		_40e‍.g.console.log(err);
	},
);

Mongoose.connect(MONGODB_URL, async (err) => {
	if (err) throw err;
	console.log("conncted to db");
});
